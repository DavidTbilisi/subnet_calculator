{
  "name": "ip-calculator",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build-js": "tsc",
    "watch-js": "tsc -w",
    "watch-pug": "pug -w src -P -o dist",
    "watch": "npm run watch-js && npm run watch-pug",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
